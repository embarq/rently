import { HideInUniversalDirective } from './hide-in-universal.directive';

describe('HideInUniversalDirective', () => {
  it('should create an instance', () => {
    const directive = new HideInUniversalDirective();
    expect(directive).toBeTruthy();
  });
});
